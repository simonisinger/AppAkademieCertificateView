name: app_akademie_certificate_view
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  app_akademie_certificate_lib:
    git: https://github.com/simonisinger/AppAkademieCertificateLib.git
  flutter:
    sdk: flutter
  encrypt: ^5.0.3
  pointycastle: ^4.0.0
  flutter_localizations:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true


dependency_overrides:
  pointycastle: ^4.0.0
